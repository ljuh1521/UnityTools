# 변경 기록

## 0.5.0

- **`refresh`가 컴파일이 실제로 걸렸는지 보고한다.** 유니티가 밖에서 고친 `.cs`를 놓칠 때가 있는데,
  그러면 옛 어셈블리가 남아 `call`이 "찾지 못했습니다"를 낸다 — **오타일 때와 구분이 안 됐다.**
  DefenceR에서 하루에 세 번 이것 때문에 헛돌았다는 보고를 받고 넣었다(2026-09-15).
  판정은 **컴파일이 시작됐는지**로 한다(전체 컴파일 시작 이벤트 + 폴링 중 `isCompiling` 관측, 둘 중
  하나라도 잡히면 걸린 것). 도메인 리로드를 넘겨야 해서 EditorPrefs에 둔다.
  **어셈블리 개수로 판정하지 않는다** — `assemblyCompilationFinished`는 유니티가 빌드 캐시로 결과를
  채울 때 안 뜬다(2026-09-15 DefenceR 실측: Csc 203개가 돌고 어셈블리를 다시 로드했는데 개수는 0,
  로그에는 `compile time=2 ms`). 개수는 잡혔을 때만 "어셈블리 N개"로 덧붙인다.
- **`recompile` 명령 신규** — `RequestScriptCompilation(CleanBuildCache)`로 캐시된 빌드 결과를
  지우고 전체를 다시 만든다. 지금까지 쓰던 "아무 `.cs`나 한 줄 건드렸다 지우기" 우회를 대신한다.
  전체 재빌드라 수 분 걸리므로 `refresh`가 "안 걸렸다"고 할 때만 쓴다.
  기본값(`None`)이 아니라 `CleanBuildCache`인 이유는 **문서상 전체 재빌드가 보장되기 때문**이다 —
  `None`은 "바뀐 스크립트만"이라 변경 감지가 실패한 상황에서 무엇을 하는지 보장이 없다.
- **판정 전에 컴파일이 시작될 틈을 준다.** 요청 직후 0.5초 만에 판정하면 곧 시작될 컴파일을
  "안 걸렸다"고 잘못 보고한다 — `recompile`이 그래서 두 번이나 무동작으로 보였다(2026-09-15 실측:
  요청과 판정 사이가 약 1초였다). `refresh`는 2초, `recompile`은 15초까지 시작을 기다린 뒤 판정한다.
  **이게 `recompile` 무동작처럼 보이던 것의 유일한 원인이었다** — 명령 자체는 처음부터 돌고 있었고,
  `Library/ScriptAssemblies`의 DLL 시각이 안 변한 것을 근거로 "무동작"이라 오판했었다. 유니티(Bee)는
  `Library/Bee/artifacts`에 만들고 내용이 달라졌을 때만 거기로 복사하므로, 소스가 그대로면 다시
  컴파일해도 그 시각은 안 변한다. **컴파일 여부는 DLL 시각이 아니라 이 보고 줄이나 `Editor.log`로 본다.**

## 0.4.0

- **Runtime 모듈 신규**(`Runtime/UI/`) — 지금까지는 `Editor/`뿐이었는데, 처음으로 런타임(빌드에도
  들어가는) 코드를 넣었다. `com.unity.ugui`가 있을 때만 컴파일(Editor/UI/와 같은 조건).
- **UI/Outline 셰이더 + `OutlineWidthModifier`** — 사각형이 아닌 실루엣(별·아치 모양 등)에도
  두께가 고른 UI 테두리. 원본 알파의 가장자리를 따라 링을 그리는 방식이라, 흔한 "같은 스프라이트를
  뒤에 깔고 사각형 틀 안쪽으로 밀어넣는" 기존 기법이 실루엣이 사각형이 아닐 때 두께가 고르지
  않아지는 문제를 해결한다. 9슬라이스(보더) 스프라이트도 지원. DefenceR에서 만든 뒤 프로젝트
  비의존이라 옮겼다(GUID 보존— DefenceR 쪽 참조는 안 깨짐).
- **`PrefabInstanceInfo` 진단 명령**(`Editor/`) — 지금 만지는 오브젝트가 중첩 프리팹 인스턴스인지,
  원본이 어디인지 알려준다. 인스턴스만 고치고 원본을 빠뜨리는 실수(DefenceR에서 실제로 한 번
  발생)를 줄이려고 추가. `call UnityTools.Editor.PrefabInstanceInfo.Dump <프리팹 경로>|<안쪽 경로>`.

## 0.3.0

- **프리팹 지킴이 — 기준선이 없으면 덮어쓰지 않고 멈춘다.** `Restore`(void)를 `TryRestore`(bool)로
  바꿨다. 이미 있는 프리팹에 기준선이 없으면 무엇이 손질인지 가릴 수 없는데, 예전에는 경고만 내고
  덮어썼다 — 그래서 손댄 팝업 두 개를 조용히 날렸다(2026-08-13). 경고는 사람이 읽고 되돌려야
  하지만, 멈추는 건 안 읽어도 지켜진다.
  **깨지는 변경**: 부르는 쪽이 반환값을 보고 저장을 건너뛰어야 한다.

## 0.2.0

DefenceR에서 스택 무관한 도구 셋을 더 뽑았다.

- **프리팹 지킴이**(`PrefabKeeper`) — 생성기가 프리팹을 다시 만들 때 사람이 인스펙터에서 고친 값과
  직접 추가한 오브젝트를 이어받는다. ugui·TMP에 의존하므로 `Editor/UI/`의 별도 어셈블리로 두고
  `com.unity.ugui`가 있을 때만 컴파일한다. 기준선 폴더(`PrefabKeeper.Root`)는 프로젝트가 정한다.
- **텍스처 프리셋**(`TexturePresetTools`) — 폴더에 프리셋을 일괄로 씌우되, 프리셋이 덮어써 버리는
  개별 설정(스프라이트 시트·9슬라이스 테두리·피벗·PPU)이 있는 텍스처는 건너뛰고 무엇을 건너뛰었는지 알린다.
  스파인 머티리얼 Straight Alpha 일괄 수정도 함께(셰이더 이름만 보므로 spine-unity에 의존하지 않는다).
- **검사 모음**(`[EditorValidator]` + `Tools/유니티 툴즈/검사`) — 검사 메서드에 속성만 붙이면
  한 메뉴에서 전부 돌린다. 검사마다 메뉴를 두면 "어느 걸 돌리지"가 되어 결국 아무도 안 돌린다.
  하나가 예외로 죽어도 나머지는 계속 돌린다.

## 0.1.1

- `call`이 오버로드 있는 메서드에서 `AmbiguousMatchException`으로 죽던 것을 고쳤다.
  이름으로 다 모은 뒤 부를 수 있는 모양(무인자 / 문자열 하나)만 고른다.
  `UnityEditor.PackageManager.Client.Resolve` 처럼 UnityEditor 쪽에 오버로드가 흔하다.
- `status.txt`를 바쁜 상태 검사보다 먼저 쓴다. 뒤에 쓰면 컴파일·임포트가 길어질 때
  갱신이 멈춰 밖에서는 브릿지가 죽은 것처럼 보인다.

## 0.1.0

DefenceR에서 스택 무관한 에디터 도구를 뽑아 첫 패키지로 만들었다.

- 에이전트 브릿지 — 명령 등록 지점(`AgentBridge.Register`)을 추가해 `spine` 같은 선택 명령을 코어 밖에서 붙이게 했다.
  타입 조회를 전체 어셈블리로 넓혀 asmdef를 쓰는 프로젝트에서도 `call`이 닿는다.
- 프리팹 미리보기 — 배경색·기준 해상도를 프로젝트가 정하게 하고, 스파인 초기화를 렌더 전 훅으로 뺐다.
- 스파인 지원은 `spine-unity`가 있을 때만 컴파일되는 별도 어셈블리로 분리.
